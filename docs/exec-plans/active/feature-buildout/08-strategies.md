# План: Стратегии и мероприятия

- Статус: completed
- Дата: 2026-05-31

## Цель

Наполнить раздел «Стратегии и мероприятия» (`src/features/strategies`) —
стратегии обслуживания, привязка к активам/классам и задачи ТОиР с интервалами.
Сейчас это заглушка.

## Контекст

- Цепочка «критичность → стратегия → мероприятия»:
  [`../../../product/main.md`](../../../product/main.md) (раздел 2, п. 5; раздел 4, п. 4).
- Зависит от: [`01-domain-model.md`](01-domain-model.md) (`Strategy`,
  `MaintenanceTask`), [`03-persistence-and-crud.md`](03-persistence-and-crud.md),
  желательно [`07-criticality.md`](07-criticality.md).

## Шаги

- [x] Список стратегий + CRUD (тип: реактивная / ППР / по состоянию / предиктивная).
- [x] Привязка стратегии к активам и/или классам оборудования (через мероприятия).
- [x] Задачи ТОиР с интервалами (date-fns для расчёта дат/периодичности).
- [x] Отображение стратегии и задач на вкладке «Стратегия и мероприятия» карточки актива.
- [x] Подсказка стратегии по уровню критичности.
- [x] i18n + визуал на токенах брендбука.

## Журнал решений

- 2026-06-04: виджет стратегий на карточке актива закладывается в фазе **1.2**
      ([`1.2-asset-card.md`](1.2-asset-card.md)); этот план наполняет данные и CRUD
      раздела «Стратегии».
- 2026-06-07: раздел выполнен. Добавлен сервис `services/strategies.ts` (выбор
      задач стратегии, расчёт следующего выполнения и периодичности через
      `date-fns`, сводка рекомендаций «класс критичности → тип стратегии» поверх
      `recommendedStrategyType` из `services/criticality`). Раздел
      `features/strategies`: вкладки «Стратегии» (таблица + CRUD, число
      мероприятий, панель рекомендаций по критичности) и «Мероприятия ТОиР»
      (таблица + CRUD, вид, интервал, привязка к активу/классу, дата следующего
      выполнения). Привязка мероприятия — режим «актив» либо «класс» (RHF + Zod,
      `superRefine`). Вкладка «Стратегия и мероприятия» карточки актива
      (`AssetStrategyTab`) показывает задачи актива и его класса. i18n-ключи
      `strategies.*`, `maintenanceTaskKind.*`, `assetCard.strategyTab.*`.

## Критерии готовности

- [x] Стратегии создаются, привязываются к активам/классам, раскладываются в задачи.
- [x] Карточка актива показывает назначенную стратегию и задачи.
- [x] `npm run lint` и `npm run build` проходят.
